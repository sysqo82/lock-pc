<Window x:Class="PCLockScreen.ReminderWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Reminder" 
        WindowStyle="None"
        ResizeMode="NoResize"
        WindowStartupLocation="CenterScreen"
        Topmost="True"
        ShowInTaskbar="True"
        Background="Transparent"
        Width="450"
        Height="250"
        AllowsTransparency="True">
    
    <Window.Resources>
        <Style TargetType="TextBlock">
            <Setter Property="Foreground" Value="#333"/>
            <Setter Property="FontSize" Value="16"/>
        </Style>
        <Style TargetType="Button">
            <Setter Property="Padding" Value="20,10"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Background" Value="#007bff"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
        </Style>
    </Window.Resources>
    
    <Border Background="White" 
            CornerRadius="12" 
            BorderBrush="#CCC" 
            BorderThickness="1"
            Padding="30">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>
            
            <!-- Icon and Title -->
            <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,20">
                <!-- Bell Icon -->
                <Viewbox Width="40" Height="40" Margin="0,0,15,0">
                    <Canvas Width="24" Height="24">
                        <Path Fill="#007bff" Data="M21,19V20H3V19L5,17V11C5,7.9 7.03,5.17 10,4.29C10,4.19 10,4.1 10,4A2,2 0 0,1 12,2A2,2 0 0,1 14,4C14,4.1 14,4.19 14,4.29C16.97,5.17 19,7.9 19,11V17L21,19M14,21A2,2 0 0,1 12,23A2,2 0 0,1 10,21"/>
                    </Canvas>
                </Viewbox>
                
                <TextBlock Text="Reminder" 
                           FontSize="24" 
                           FontWeight="Bold" 
                           VerticalAlignment="Center"/>
            </StackPanel>
            
            <!-- Reminder Message -->
            <Border Grid.Row="1" 
                    Background="#dadfe4" 
                    CornerRadius="8" 
                    Padding="20"
                    Margin="0,0,0,20">
                <TextBlock x:Name="ReminderMessage" 
                           Text="Reminder message will appear here"
                           TextWrapping="Wrap"
                           FontSize="18"
                           FontWeight="SemiBold"
                           HorizontalAlignment="Center"
                           VerticalAlignment="Center"
                           TextAlignment="Center"/>
            </Border>
            
            <!-- Dismiss Button -->
            <Button Grid.Row="2" 
                    Content="Dismiss" 
                    Click="Dismiss_Click"
                    HorizontalAlignment="Center"
                    Width="150"/>
        </Grid>
    </Border>
</Window>
