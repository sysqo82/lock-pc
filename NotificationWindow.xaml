<Window x:Class="PCLockScreen.NotificationWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:local="clr-namespace:PCLockScreen"
        Title="PC Lock Notification"
        FlowDirection="{Binding Source={x:Static local:Loc.Instance}, Path=FlowDirection}" 
        Width="450" Height="240"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        Topmost="True"
        ShowInTaskbar="False"
        ResizeMode="NoResize"
        Loaded="Window_Loaded">
    
    <Border x:Name="MainBorder"
            Background="#FFFFFFFF" 
            BorderBrush="#FFFFA500" 
            BorderThickness="3" 
            CornerRadius="8"
            Margin="10">
        <Border.Effect>
            <DropShadowEffect Color="Black" Opacity="0.5" BlurRadius="15" ShadowDepth="5"/>
        </Border.Effect>
        
        <Grid Margin="20">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>
            
            <!-- Title -->
            <TextBlock x:Name="TitleText"
                       Grid.Row="0"
                       Text="{Binding Source={x:Static local:Loc.Instance}, Path=Strings.Notification_Title}" 
                       FontSize="22" 
                       FontWeight="Bold"
                       Foreground="#FFFF8C00"
                       Margin="0,0,0,10"/>
            
            <!-- Message -->
            <TextBlock x:Name="MessageText"
                       Grid.Row="1"
                       Text="{Binding Source={x:Static local:Loc.Instance}, Path=Strings.Notification_Message}"
                       FontSize="16"
                       FontWeight="SemiBold"
                       TextWrapping="Wrap"
                       Foreground="#FF000000"
                       LineHeight="24"/>
            
            <!-- Close button -->
            <Button Grid.Row="3"
                    Content="{Binding Source={x:Static local:Loc.Instance}, Path=Strings.Notification_Dismiss}"
                    Click="Dismiss_Click"
                    Width="100"
                    Height="32"
                    FontSize="14"
                    FontWeight="SemiBold"
                    HorizontalAlignment="Right"
                    Margin="0,15,0,0"
                    Padding="10,5"/>
        </Grid>
    </Border>
</Window>
